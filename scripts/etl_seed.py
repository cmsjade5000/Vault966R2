"""Archived importer script.

The original bulk importer now lives under legacy/etl/etl_seed.py. This stub remains
so existing docs/imports fail fast with guidance.
"""

raise RuntimeError("etl_seed.py has moved to legacy/etl/etl_seed.py (archived)")
